name: "develope"
version: "0.1.1"
usage: "Watch and upgrade files inside a release as you code"
description: |-
  Watch and upgrade files inside a release as you code.
command: "$HELM_PLUGIN_DIR/develope.sh"
